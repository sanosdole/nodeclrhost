{
  "name": "coreclr-hosting",
  "version": "0.1.0-dev.1",
  "description": "Hosting dotnet coreclr in node",
  "main": "bindings.js",
  "gypfile": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/sanosdole/nodeclrhost.git"
  },
  "homepage": "https://github.com/sanosdole/nodeclrhost",
  "engines": {
    "node": ">=12"
  },
  "keywords": [
    "dotnet"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "configure": "node-gyp configure",
    "build": "npm run configure && npm run build:debug && npm run build:release",
    "build:debug": "node-gyp build --debug",
    "build:release": "node-gyp build",
    "clean": "node-gyp clean",
    "build-testapp": "node build-scripts/dotnet-publish.js test/TestApp/TestApp.csproj test/bin/published",
    "test": "npm run build && npm run build-testapp && mocha",
    "test-only": "npm run build-testapp && mocha",
    "mocha": "mocha",
    "prepare": "npm run build",
    "install": "prebuild-install || node-gyp rebuild"
  },
  "author": "Daniel Martin",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^6.2.2",
    "node-gyp": "^6.0.1"
  },
  "dependencies": {
    "node-addon-api": "2.0.0",
    "prebuild-install": "^5.3.3"
  }
}
